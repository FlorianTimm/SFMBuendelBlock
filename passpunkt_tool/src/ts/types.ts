export type Bild = {
    bid: number,
    kamera: number,
    url: string
}

export type PasspunktPosition = {
    name: string,
    passpunkt: number,
    image: number,
    x: number,
    y: number
}

export type Passpunkt = {
    name: string,
    pid: number
}
